swagger: "2.0"
info:
  title: "Logistics Control Tower API"
  description: "API Gateway for AI Logistics Agent"
  version: "1.0.0"
schemes:
  - "https"
consumes:
  - "application/json"
produces:
  - "application/json"
paths:
  /query:
    post:
      summary: "Process a logistics query"
      operationId: "processQuery"
      x-google-backend:
        address: "https://logistics-agent-backend-255413983349.us-central1.run.app"
        deadline: 60.0
      responses:
        200:
          description: "Success"
          schema:
            type: object
            properties:
              summary:
                type: string
              sql:
                type: string
              error:
                type: string
  /health:
    get:
      summary: "Health Check"
      operationId: "healthCheck"
      x-google-backend:
        address: "https://logistics-agent-backend-255413983349.us-central1.run.app"
        deadline: 60.0
      responses:
        200:
          description: "Success"
  /sample:
    get:
      summary: "Get Sample Data"
      operationId: "getSample"
      x-google-backend:
        address: "https://logistics-agent-backend-255413983349.us-central1.run.app"
        deadline: 60.0
      responses:
        200:
          description: "Success"
